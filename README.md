# lazyos

A TUI to manage kernel agnostic operating systems.

## Setup

**`/config.yml`**

machines:

- id : `<id>`
  label: `<label>`
  username: `<username>`
  hostname: `<hostname>`
  port: `<port>`
  add_keys_to_agent: `true/false`

**`/.env`**
PASSWORD\_`<id>`=`<password>`
